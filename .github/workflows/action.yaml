name: Must run on customer runner

on: [push]

env:
  NUMERO_N: 1
jobs:
  init:
    runs-on: [ubuntu-latest]
    
    steps:
      - name: control numero
        id: STEP1
        env: 
          CIAO_MONDO: ciao
        run: echo "$CIAO_MONDO"
      - name: prova
        run: echo "${{ steps.STEP1.env.CIAO_MONDO }}"
     #   if: ${{ env.NUMERO_N == 1 }}
     #   run: |
     #     echo "ok SOCIO"
     # - name: control
   #     shell: bash
     #   if: ${{ env.NUMERO_N == 1 }}
     #   run: | 
     #     echo "uscire!!"
     #     exit 1
     # - name: finish
     #   run: echo "Bella socio"
